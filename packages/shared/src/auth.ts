/**
 * X-Service-Key authentication middleware
 * TODO: Implement in Phase 1
 */

export function createAuthMiddleware(serviceKey: string) {
  // Placeholder
  return (req: any, res: any, next: any) => {
    console.log('Auth middleware - TODO');
    next();
  };
}
